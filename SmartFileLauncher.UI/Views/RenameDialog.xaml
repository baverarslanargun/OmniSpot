<Window x:Class="SmartFileLauncher.UI.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yeniden Adlandır"
        Width="450"
        Height="210"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="#FAFBFF">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   x:Name="LabelText"
                   Text="Yeni ad girin:" 
                   FontSize="13" 
                   Foreground="#334155"
                   Margin="0,0,0,10"/>
        
        <TextBox Grid.Row="1" 
                 x:Name="NameTextBox"
                 FontSize="14"
                 Padding="10,8"
                 BorderBrush="#CBD5E1"
                 BorderThickness="1"
                 Background="White">
            <TextBox.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </TextBox.Resources>
        </TextBox>
        
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="İptal" 
                    Width="80" 
                    Height="32"
                    Margin="0,0,10,0"
                    Background="#F1F5F9"
                    BorderBrush="#E2E8F0"
                    Foreground="#64748B"
                    Click="Cancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Tamam" 
                    Width="80" 
                    Height="32"
                    Background="#6366F1"
                    BorderBrush="#4F46E5"
                    Foreground="White"
                    Click="OK_Click"
                    IsDefault="True">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
