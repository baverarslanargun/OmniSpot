<Window x:Class="SmartFileLauncher.UI.Views.ThumbnailPinConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thumbnailleri hızlı yükle" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="#F5F5F7" FontFamily="Segoe UI" FontSize="13">
    <StackPanel Margin="24">
        <TextBlock Text="Bu özellik RAM kullanımını artırabilir" FontSize="17" FontWeight="SemiBold" TextWrapping="Wrap"/>
        <TextBlock x:Name="FolderText" TextWrapping="Wrap" Margin="0,12,0,12" Foreground="#515159"/>
        <TextBlock Text="Bu klasörde ve alt klasörlerinde gezinirken yüklenen küçük resimler, OmniSpot boşta kaldığında da RAM'de tutulur. Güvenli bellek sınırı dolarsa en eski kullanılan görseller çıkarılabilir."
                   TextWrapping="Wrap"/>
        <CheckBox x:Name="DoNotShowAgain" Content="Bir daha gösterme" Margin="0,20,0,20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="İptal" IsCancel="True" MinWidth="85" Padding="12,7" Margin="0,0,10,0"/>
            <Button Content="Tamam" IsDefault="True" MinWidth="85" Padding="12,7" Click="Confirm_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
